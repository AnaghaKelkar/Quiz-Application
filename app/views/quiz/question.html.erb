<% if @message.present? %>
  <div class="container" style = "margin-top: 5%">
    <div class="row">
      <center>
        <p class = "positive-review"><b><%= @message %></b></p>
        <p style = "margin-top: 2%"><%= link_to 'Select another category', categories_path %></p>
      </center>
    </div>
  </div>
<% elsif @question.present? %>
  <%= render 'quiz_question', question: @question, choices: @choices, category: @category, quelist: @quelist, quiz: @quiz %>
<% else %>
  <%= render 'quiz_thankyou', score: @quiz_score, quiz: @quiz %>
<% end %>
